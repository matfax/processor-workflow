buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.netflix.nebula:gradle-lint-plugin:11.4.0'
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }

    apply plugin: 'nebula.lint'

    gradleLint.rules = ['all-dependency']
}
