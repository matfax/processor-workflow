buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.netflix.nebula:gradle-lint-plugin:13.1.7'
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }

    apply plugin: 'nebula.lint'

    gradleLint.rules = ['all-dependency']
}
